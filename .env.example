# Copy to .env for LOCAL development only. Never commit the real .env file
# (it is already in .gitignore). On Render, do not use this file at all —
# set GEMINI_API_KEY directly in the dashboard's Environment tab instead.
GEMINI_API_KEY=your_key_from_google_ai_studio_here
